import { useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';

const Termek = () => {
    const { id } = useParams();
   
    return (
        <>
            
        </>
    );
};

export default Termek;
